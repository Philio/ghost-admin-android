<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Styleable for ScrimInsetsFrameLayout -->
    <declare-styleable name="ScrimInsetsScrollView">
        <attr name="scrimInsetForeground" format="reference|color" />
    </declare-styleable>

    <!-- Styleable for BezelImageView -->
    <declare-styleable name="BezelImageView">
        <attr name="maskDrawable" format="reference" />
        <attr name="borderDrawable" format="reference" />
        <attr name="desaturateOnPress" format="boolean" />
    </declare-styleable>

</resources>